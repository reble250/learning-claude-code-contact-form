# Claude Code学習メモ（ブラウザテスト自動化セッション）

本メモは、`テストケース.md`をもとに「ブラウザテスト項目の設計 → 報告書テンプレート作成 → Playwrightによる実施 → 結果報告書の作成」までを一気通貫でClaude Codeにやらせたセッションの記録と、そこから得られた学びをまとめたもの。対象読者は自分自身（Claude Codeの使い方を学びたい人）。

## 1. セッションでやったこと

| 順番 | 指示（要約） | 成果物 |
|---|---|---|
| 1 | `テストケース.md`（curl/tinkerによるHTTPレベルの手動テスト結果）をもとに、ブラウザ操作ベースのテスト項目を設計 | `myproject/docs/ブラウザテスト.md` |
| 2 | スクリーンショット証跡付きで先方に提出する報告書の雛形を作成 | `myproject/docs/ブラウザテスト報告書テンプレート.md` |
| 3 | 1で設計した全36項目をPlaywright MCPで実際に操作し、雛形に沿って結果をまとめる | `myproject/docs/ブラウザテスト結果.md` + スクリーンショット39枚（`myproject/docs/screenshots/`） |

ポイントは、**「項目設計」「雛形」「実施・記録」を3つの独立した指示に分けたこと**。1回で「テストして報告書作れ」と頼むより、途中成果物（ブラウザテスト.md／テンプレート）をレビューできる区切りができ、Claude Code側も既存ファイルを参照しながら次工程を進められた。

## 2. 使ったClaude Codeの機能・ツール

- **Read / Write / Edit**: 既存の`テストケース.md`・Bladeテンプレート（`resources/views/**`）・`routes/web.php`を読み、画面項目やURLを裏取りしてからテスト項目を設計した。想像でURLや項目名を書かず、実装を読んでから文書化するのが基本動作。
- **Bash**: `php artisan tinker`でDBの件数・ステータス内訳を確認、`curl`でサーバー起動確認、`find`でファイル探索、CSVの中身を`wc -l`/`cut`で検証、など「ブラウザに映らない裏側の事実確認」に多用した。
- **Playwright MCP（`mcp__playwright__browser_*`）**: 実際のブラウザ操作。`browser_navigate`／`browser_fill_form`／`browser_click`／`browser_select_option`／`browser_evaluate`／`browser_take_screenshot`を組み合わせて36項目を1つずつ実施した。
- **ToolSearch**: Playwrightツールはデフォルトではスキーマがロードされていないdeferred toolだったため、`ToolSearch(select:...)`で明示的にスキーマを取得してから使えるようになった。
- **TaskCreate / TaskUpdate**: 36項目という量があったため、「フォーム側」「管理側」「CSV」「結果ドキュメント作成」の4タスクに分けて進捗を管理した。

## 3. 技術的な気づき（つまずき・工夫）

### スクリーンショットの保存先は期待通りにならないことがある
`browser_take_screenshot`に相対ファイル名を渡すと、Playwright MCPサーバーの実行時カレントディレクトリ（今回はリポジトリのルート）に直接保存された。`.playwright-mcp/`配下に入ると思い込んでいたが違った。最終的に`find`でファイルを洗い出し、`docs/screenshots/`へ`mv`でまとめて整理した。
→ **学び**: MCPツールの「ファイルを保存する」系パラメータは、保存先の実際の挙動を一度確認してから大量生成に入った方が手戻りが少ない。

### `file://`プロトコルはPlaywrightブラウザからブロックされる
ダウンロードしたCSVをブラウザで開いて中身をスクリーンショットしようとしたが、`file://`へのnavigateはセキュリティ上拒否された。
→ 代替として、ダウンロードしたファイルをBashで読み取り、`browser_evaluate`でDOMに内容を注入した上でスクリーンショットを撮る、という回避策を取った。「ブラウザで見た目を証跡化したい」ときは、この注入テクニックが使える。

### ブラウザネイティブの入力制約はUI操作だけでは破れない
`<input type="date">`や`<select>`は、ユーザーが直接不正な値を入力できないようブラウザ側で制約されている。境界値テスト（不正な日付形式・不正なステータス値）を再現するには、`browser_evaluate`で`input.type = 'text'`に変更したり`<option>`を動的追加したりして、開発者ツールでのDOM改変に相当する操作を行う必要があった。
→ **学び**: 「ブラウザで再現できないはずのケース」をテストする場合は、DOM操作で制約を迂回する手法を使えばよい。ただし「通常操作では起こり得ない」という前提を報告書側にも明記しておかないと、再現性のない不具合報告に見えてしまう。

### 破壊的なDB操作は自動でブロックされる
テストで作成したレコードを片付けるために`tinker`から`delete()`を実行しようとしたところ、「Auto mode classifier」によって拒否された。Claude Code側が「これはユーザー確認なしに実行してよい操作か」を判断し、破壊的操作は止める設計になっていることを実地で確認できた。
→ 止められた際は無理に回避しようとせず、状況をユーザーに説明して判断を委ねる（実際、最終報告でその旨をユーザーに申し送りした）。

### httpOnlyのセッションCookieはJavaScriptから操作できない
「新規セッションで完了画面に直接アクセスする」テスト（BF12）を、Cookieの`document.cookie`クリアで擬似的に再現しようとしたが、Laravelのセッションクッキーは`httpOnly`属性がついておりJSからは見えない・消せない。ブラウザのセキュリティモデルはテスト自動化のときにも普通に効いてくる。
→ 完全な新規セッションを再現するには、別のブラウザコンテキスト（シークレットタブ相当）を使うなど、Cookie操作以外の手段が必要になる。今回はその制約を報告書に正直に書いた。

## 4. 進め方・プロンプト設計の学び

- **既存ドキュメントを「土台」にして次を作らせる**: `テストケース.md`→`ブラウザテスト.md`→`ブラウザテスト結果.md`と、前工程の成果物を次工程の入力にする指示の連鎖が機能した。Claude Codeは指定されたファイルを読み込んでからでないと項目の粒度や表現を合わせられないため、「〜をもとに」という参照先の明示が効いている。
- **雛形（テンプレート）を独立した成果物にする価値**: 先に「書き方の雛形」を作らせてから「その雛形に従って結果を埋める」という2段階にしたことで、最終報告書のフォーマットが安定した。雛形なしで一気に「結果をまとめて」と頼むと、項目ごとの粒度や見出し構造がぶれやすい。
- **量がある定型作業はタスク管理と相性がよい**: 36項目 × スクリーンショット付きという単純作業の反復では、`TaskCreate`/`TaskUpdate`で区切りを可視化しておくと、長い実行の中でも「今どこをやっているか」が自分にもツールにも分かりやすかった。
- **裏取りを怠らない**: ブラウザ上の見た目だけでなく、`tinker`でDB値を確認する／DOMを`evaluate`で読んでエスケープされているか確認する、といった「画面の裏側の事実確認」をセットで行うことで、スクリーンショットだけでは証明できない部分（XSSエスケープ、DB未更新など）まで裏付けを取れた。
- **テストデータの副作用は隠さず報告する**: 一連のブラウザ操作自体がテスト対象のDBにレコードを追加する副作用を持つ。その結果、以前のテストケース（`テストケース.md`のA10）と異なる件数になった箇所があったが、原因を明記した上で「機能自体は正しい」と結論づけた。自動化されたテストほど、実行環境への副作用を意識して報告する必要がある。

## 5. 次に活かせそうなこと

- MCPツールでファイル出力する際は、最初の1回で保存場所を確認してから量産に入る。
- ブラウザの標準的な入力制約を破りたいときは`browser_evaluate`でのDOM改変が使える（ただし「通常操作では再現不可」と明記する）。
- 破壊的操作（DB削除、force push等）はブロックされる前提で、後片付けが必要な作業をする場合はユーザーに確認を仰ぐ運用にしておく。
- 多段階・大量の定型作業は「土台ドキュメント → 雛形 → 実施・記録」の3段階に分け、タスク管理ツールで進捗を可視化すると安定する。
