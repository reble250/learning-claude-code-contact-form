# Claude Code学習メモ（スキル編）

前回の`Claude Code学習メモ.md`（ブラウザテスト自動化セッション）に続き、同じ会話の中で「Claude Codeのスキル（`.claude/skills/`）を自作する」ことに取り組んだ記録と、そこから得られた学びをまとめる。

## 1. セッションでやったこと

| 順番 | やったこと | 結果 |
|---|---|---|
| 1 | ブラウザテスト実行の手順を`browser-test`スキルとして作成（`myproject/.claude/skills/`配下） | 作成はできたが、この時点では動作未検証 |
| 2 | 作成したスキルを自己レビュー（「妥当か？」という自問） | 内部矛盾（部分再実行の指示と実装の不一致）、置き場所の未検証、冗長な記述などを自分で発見 |
| 3 | サポートファイル（`references/`分割）の要否を検討 | 現状のボリューム（60行程度）では不要と判断。スクリーンショット収集用スクリプトの追加案は提示したがユーザー判断で見送り |
| 4 | 「テストデータ作成スキル」を新規作成。件数を引数で渡せるようにする、という要件 | 既存データの扱い（削除して作り直すか／追加するか）をAskUserQuestionでユーザーに確認 → 「毎回削除してから作成」を選択 |
| 5 | 実装: `database/seeders/ContactSeeder.php`を`run(int $count = 100)`に変更（`Contact::truncate()`後に指定件数を男女比半々で作成）、新規Artisanコマンド`app/Console/Commands/SeedContacts.php`（`contacts:seed {count=100}`）を追加 | 20件・7件（奇数）・0件（異常系）・100件（復元）で動作確認済み |
| 6 | `seed-test-data`スキルを作成 | 同上、`myproject/.claude/skills/`配下に作成 |
| 7 | 「利用可能なスキルを教えて」と聞かれ、標準スキルとカスタムスキルを一覧化 | この時点ではカスタム2スキルが本当に呼べるかは未検証と正直に回答 |
| 8 | 実際に`Skill`ツールで`browser-test`・`seed-test-data`・`myproject:browser-test`を呼び出してみた | すべて`Unknown skill`エラー |
| 9 | 置き場所の仮説（プロジェクトフォルダ＝サブディレクトリではなくリポジトリルートでは？）を立て、`myproject/.claude/skills/`→リポジトリルート直下`.claude/skills/`へ移動して再試行 | それでも`Unknown skill`。置き場所の問題ではないと判明 |
| 10 | `claude`本体の実行バイナリを`strings`コマンドで文字列抽出し、スキル関連の埋め込み文言・スキーマ定義・エラーメッセージを直接調査 | スキャン対象ディレクトリ、フロントマターの全フィールド、公式の作成ガイドライン、「次回セッションで自動読み込み」という確定情報を発見 |
| 11 | 調査結果をもとに、スキルの置き場所の違い（プロジェクトルート／サブディレクトリ／ホームディレクトリ／プラグイン）とフロントマターの意味をユーザーに説明 | — |

## 2. 技術的な学び（Claude Codeのスキル仕様）

### スキルの検出は「セッション開始時に一度だけ」
最大の学びはこれに尽きる。`claude`バイナリ内のCLIヘルプ文言に

> Scaffold a new plugin at `~/.claude/skills/<name>/` (**auto-loads next session** as `<name>@skills-dir`)

と明記されており、**会話の途中で`.claude/skills/`にファイルを追加しても、そのセッション内では使えない**。新しいセッション（新しい会話）を開始して初めて認識される。今回、置き場所を変えても直らなかったのはこれが原因だった。
→ **学び**: 「動くはずなのに動かない」ときに、原因を1つの仮説（置き場所）に決め打ちせず、検証可能な形で複数の仮説を切り分けたのが有効だった（後述の「調査の進め方」参照）。

### スキルの置き場所は4種類、役割が違う
| 置き場所 | スコープ |
|---|---|
| `<リポジトリルート>/.claude/skills/` | そのプロジェクトのみ。gitで管理すればチーム共有可能 |
| `<サブディレクトリ>/.claude/skills/`（モノレポ向け） | そのサブディレクトリ配下の作業時のみ有効。`apps/billing:run-billing`のようにパス接頭辞付きで一覧に出る |
| `~/.claude/skills/` | 自分専用。全プロジェクト共通だがgit管理外で他人とは共有されない |
| プラグインスキル（`plugin:skill`） | `/plugin install`で導入。マーケットプレイス経由で配布・共有できる |

今回作成した2つは「プロジェクトルート」に置くのが正解で、`myproject/`という一段下のサブディレクトリに最初置いたのは誤りだった（動かなかった直接の原因ではなかったが、正しい場所ではあった）。

### フロントマターは想像以上に多機能
`name`/`description`だけでなく、`allowed-tools`（有効時に使えるツールを制限）、`disallowed-tools`、`model`（このスキル実行中のモデル上書き）、`context: inline/fork`（会話にそのまま展開するかサブエージェントとして起動するか）、`paths`（特定のglobパターンのファイルを触っているときだけ自動発火）、`when_to_use`（`description`とは別に発火判断の補助説明を持てる）など、スキルを「ただの手順書」から「実行環境をその場で切り替えるプラグイン」に近づけるフィールドが揃っている。
→ 今回作った2スキルはこれらを使っていない素朴なものだったが、例えば`seed-test-data`に`allowed-tools: Bash`を指定して余計なツールを使わせない、といった絞り込みが可能だとわかった。

### 公式のスキル作成ガイドラインが存在する
バイナリに埋め込まれていた文言:
> Skills use progressive disclosure: lean SKILL.md body (under 3,000 words), detailed content in `references/`. Frontmatter description must be third-person with specific trigger phrases. Skill bodies are instructions FOR Claude, not messages to the user — write them as directives.

- 本体は軽量に保ち、詳細は`references/`へ逃がす（プログレッシブ・ディスクロージャー）
- `description`は三人称・トリガーフレーズ入りで書く
- 本文はユーザー向けの説明文ではなく、Claude自身に対する指示文として書く
- 公式の`skill-creator`プラグイン（`/plugin install skill-creator@claude-plugins-official`）を使えば、スキルの新規作成や既存スキルの評価（evals）を任せられる

## 3. 調査の進め方から得た学び

- **「わからないなら実行バイナリを直接読む」という手段があった**: ドキュメントが手元にない状況で、`strings`コマンドで実行ファイルからテキストを抜き出し、埋め込まれたヘルプ文言・エラーメッセージ・スキーマ定義を読むことで、憶測ではなく一次情報に近い形で仕様を特定できた。「わからない」で終わらせず、調べる手段を探すこと自体が一つの学び。
- **仮説は1つずつ検証する**: 「置き場所が悪いのでは」という仮説を立てたら、実際にファイルを移動してから再度呼び出しテストを行い、仮説が誤りだったことを確認してから次の仮説（セッションキャッシュ）に進んだ。決め打ちで説明せず、動く根拠を積み上げてから結論づけた。
- **自分の成果物を批判的にレビューする姿勢**: `browser-test`スキルを作った直後に「妥当か」と問われた際、内部矛盾（部分再実行の指示と実装のズレ）や未検証の前提を自分で洗い出せた。作って終わりにせず、後から読み返すと粗が見つかるという前提で確認する価値があった。
- **破壊的な実装変更は先にユーザーに確認する**: `ContactSeeder`を「毎回truncateしてから作り直す」仕様にする前に、既存データを消してよいかをAskUserQuestionで確認した。DBを触るスキルは特に、既定動作（削除か追加か）をユーザーに決めてもらうべき典型的な分岐点だった。
- **実装は作って終わりにせず、境界値で動作確認する**: `contacts:seed`コマンドは20件・奇数7件・異常系0件・復元100件と複数パターンで実行し、件数どおりに作成されること／既存データが消えること／不正な値がエラーになることを確認してからスキル化した。

## 4. 次に活かせそうなこと
- 新しく作ったスキルが動かないときは、まず「新しいセッションで試したか」を疑う。置き場所やフォーマットのデバッグはその後でよい。
- スキルを置くなら、モノレポでない限りは基本的に**リポジトリルート直下**の`.claude/skills/`に置く。個人用の汎用スキルは`~/.claude/skills/`。
- スキルの`description`は三人称・トリガーフレーズ入りで書く。本文はClaude向けの指示文として書く。
- スキル本体が育ってきたら`references/`分割を検討する（今回の2スキルはまだその規模ではない）。
- 公式の`skill-creator`プラグインの存在を覚えておき、次にスキルを作る／既存スキルを評価するときは使ってみる。
- 未知の仕様を調べる手段として、`strings`での実行バイナリ調査という選択肢がある（ただし一次情報が手に入る場合はそちらを優先すべき最終手段的な方法である点は留意）。
