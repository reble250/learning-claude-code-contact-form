# お問い合わせフォーム テストケース

## 概要
- 対象: お問い合わせフォーム（入力側）／管理画面（管理側）
- 実施日: 2026-07-31
- 実施方法: `php artisan serve`で起動したローカルサーバーに対し、curlによるHTTPリクエストと`php artisan tinker`によるDB直接検証を実施（手動E2E確認）
- 前提データ: `database/seeders/ContactSeeder.php`で投入した100件のダミーデータ（詳細は`docs/仕様書.md`5.5節）を利用
- 判定基準: OK=期待結果と実際の結果が一致、NG=不一致

## 1. フォーム入力側

| No | テスト項目 | 手順 | 期待結果 | 実際の結果 | 判定 |
|---|---|---|---|---|---|
| F01 | 入力画面表示 | `GET /contact` | 200 OKでフォームが表示される | 200 OK | OK |
| F02 | 必須項目未入力 | 名前・メール・件名・本文すべて空で `POST /contact/confirm` | 4項目すべてで「〜を入力してください。」エラー | 4項目すべて表示された | OK |
| F03 | メールアドレス形式不正 | `email=not-an-email` で送信 | 「メールアドレスの形式で入力してください。」 | 表示された | OK |
| F04 | 名前が255文字超過 | `name`に256文字を入力 | 「名前は255文字以内で入力してください。」 | 表示された | OK |
| F05 | 本文が2000文字超過 | `body`に2001文字を入力 | 「本文は2000文字以内で入力してください。」 | 表示された | OK |
| F06 | バリデーションエラー時の入力保持 | 一部不正な状態で送信後、入力画面の値を確認 | `old()`により送信した値が入力欄に残る | 名前・件名・本文とも保持されていた | OK |
| F07 | XSS対策（エスケープ） | 件名に`<script>alert(1)</script>`、本文に`<b>bold</b>`を入力し確認画面を表示 | HTMLタグがエスケープされ`&lt;script&gt;`等として表示され、タグとして実行されない | エスケープされた状態で出力されていた（生の`<script>`タグは出現せず） | OK |
| F08 | 確認画面の表示内容 | 正常な値で`POST /contact/confirm` | 入力した氏名・メール・件名・本文が確認画面にそのまま表示される | すべて一致 | OK |
| F09 | 「戻る」で入力保持 | 確認画面から`POST /contact/back` | 入力画面へリダイレクトされ、入力値が保持される | 保持されていた | OK |
| F10 | 送信・保存 | 確認画面から`POST /contact` | DBに新規レコードが作成され、`status=new`（デフォルト）、`ip_address`/`user_agent`が自動記録される。完了画面へリダイレクト | レコード作成・`status=new`・IP/UA記録・完了画面表示すべて確認 | OK |
| F11 | 完了画面の再アクセスガード | 送信直後に`GET /contact/thanks`を再度リクエスト（ページリロード相当） | 完了画面は表示されず入力画面へリダイレクトされる（セッションフラグは1回で消費） | 入力画面へリダイレクトされた | OK |
| F12 | 完了画面への直接アクセス | 未送信の新規セッションで`GET /contact/thanks`に直接アクセス | 入力画面へリダイレクトされる | 入力画面へリダイレクトされた | OK |

## 2. 管理側

| No | テスト項目 | 手順 | 期待結果 | 実際の結果 | 判定 |
|---|---|---|---|---|---|
| A01 | 未ログイン時の一覧アクセス制御 | 未ログインで`GET /admin/contacts` | `/login`へ302リダイレクト | 302で`/login`へリダイレクト | OK |
| A02 | 未ログイン時の詳細アクセス制御 | 未ログインで`GET /admin/contacts/1` | `/login`へ302リダイレクト | 302で`/login`へリダイレクト | OK |
| A03 | ログイン失敗 | 誤ったパスワードで`POST /login` | ログイン画面に戻り「メールアドレスまたはパスワードが正しくありません。」を表示 | 表示された | OK |
| A04 | ログイン成功 | 正しいメール・パスワードで`POST /login` | 管理画面（一覧）へリダイレクトされ、ログアウトボタンが表示される | リダイレクトされ「お問い合わせ管理」「ログアウト」表示を確認 | OK |
| A05 | 一覧表示・ページネーション | ログイン済みで`GET /admin/contacts` | 新着順で最大20件表示される | 20行表示（全100件中） | OK |
| A06 | 氏名部分一致検索 | `name=山田`で検索 | 「山田」を含む氏名のみ絞り込まれる | 該当2件のみ表示 | OK |
| A07 | 該当なし検索 | 存在しない氏名で検索 | 「該当するお問い合わせが見つかりません。」を表示 | 表示された | OK |
| A08 | ステータス複数選択検索 | `status[]=new`, `status[]=resolved`を指定 | 選択したステータスのみ（OR条件）表示され、`対応中`は含まれない | 一覧内は`新規`8件・`解決済み`12件のみで`対応中`は0件（tbody内で確認） | OK |
| A09 | 受付日時期間検索 | `date_from`/`date_to`で1ヶ月分を指定 | 指定期間内の受付日時のレコードのみに絞り込まれる（DB直接集計と一致） | DB集計49件に対し1ページ目20件表示、範囲内であることを確認 | OK |
| A10 | 検索条件の組み合わせ（AND） | `name=山田`かつ`status[]=new`で検索 | 両条件を満たすレコードのみ（該当が無ければ0件） | 該当0件（DB直接集計でも「山田」該当者は全員`resolved`のため0件が正しい） | OK |
| A11 | 検索条件クリア | 検索条件なしで`GET /admin/contacts` | 全件（ページ内20件）表示に戻る | 20件表示 | OK |
| A12 | ページネーションへの検索条件引き継ぎ | 検索条件付きで2ページ目のリンクを確認 | リンクに`date_from`/`date_to`のクエリが含まれる | `?date_from=...&date_to=...&page=2`のリンクを確認 | OK |
| A13 | 詳細画面の表示内容 | `GET /admin/contacts/{id}` | 受付日時・名前・メールアドレス・件名・本文が表示される | すべて表示された | OK |
| A14 | 存在しないIDへのアクセス | `GET /admin/contacts/999999` | 404 Not Found | 404 | OK |
| A15 | ステータス更新（正常系） | 詳細画面から`PATCH /admin/contacts/{id}`で`status=in_progress`を送信 | 更新が成功し「ステータスを更新しました。」が表示され、DBの値も変わる | 表示され、DB値も`in_progress`に更新されていた | OK |
| A16 | ステータス更新（異常系：不正な値） | `status=invalid_status_value`（enumに存在しない値）を送信 | バリデーションエラーとなり、DBの値は変更されない | 302（バリデーションエラーで差し戻し）、DB値は更新前のまま | OK |
| A17 | 未ログイン・CSRFトークンなしでのステータス更新 | セッション・CSRFトークンなしで直接`PATCH /admin/contacts/1`を送信 | 更新が拒否される | 419（CSRFトークン不一致）で拒否された（authミドルウェアに到達する前にCSRF層で遮断） | OK |
| A18 | ログアウト後の再アクセス制御 | `POST /logout`後に`GET /admin/contacts`へアクセス | 再び`/login`へリダイレクトされる | 302で`/login`へリダイレクト | OK |

## 3. 結果サマリー
- フォーム入力側: 12件中12件OK
- 管理側: 18件中18件OK
- NG項目: なし

## 4. 備考・今後の検討事項
- A17はCSRF保護（419）によって結果的にブロックされており、`auth`ミドルウェア自体の未ログインリダイレクト（302→`/login`）は検証できていない。CSRFトークンありでセッションのみ未ログインのケースを別途検証すると、より厳密に`auth`ミドルウェアの挙動を確認できる。
- 自動化されたFeature test（PHPUnit）は未整備。本ドキュメントの手順はPHPUnitの`assertSee`/`assertRedirect`等に落とし込める内容のため、CI化する場合はテストコードとして移植することを推奨する。
- テスト実施にあたり作成した検証用レコード（`kakunin@example.com`等）はテスト後に削除し、DBは検証前と同じ100件の状態に復元済み。
